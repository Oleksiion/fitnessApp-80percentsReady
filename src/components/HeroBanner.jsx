import { Box, Stack } from "@mui/material";
import React from "react";

const HeroBanner = () => {
  return (
    <Box
      height={300}
      sx={{
        background: "green",
      }}
    >
      <Stack>HeroBanner</Stack>
    </Box>
  );
};

export default HeroBanner;
